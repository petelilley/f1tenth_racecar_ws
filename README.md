# UB F1Tenth Club - F1Tenth ROS2 Racecar Workspace

To checkout submodules:
```
git submodule update --init --recursive
```

To update all submodules to current versions:
```
git submodule foreach git pull
```
